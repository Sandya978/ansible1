---
- name: activity1
  hosts: all
  become: true
  tasks:
    - name: apacheinstallation
      ansible.builtin.apt:
        name: 
          - apache2
        state: "present"
      ansible.builtin.systemd_service:
        name:
          - service
        enabled: "true"
        state : "started"
        